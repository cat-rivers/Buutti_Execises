const toy = "squishmallow";
const game = "zelda: ocarina of time";

if (toy.length > game.length) {
  console.log("First word is longer than the second word");
} else if (toy.length < game.length) {
  console.log("Second word is longer than the first one ");
}
